import { isOutsideTopOfLayout, waitForMillisecond } from "./utils.js";

export default class BulletInst extends globalThis.ISpriteInstance {
    constructor() {
        super();
    }

    update = (runtime) => {   
    };
}